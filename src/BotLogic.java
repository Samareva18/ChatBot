/*public class BotLogic {

    public static String PrintHelp(){
        return
    }
    public static String GetAnswer(String userMessage, Dialog dialog){
        //switch(case)
        // help // возвращаем текст справки
        // start Start() // начало диалога
        // letter Letter() // вводится буква для проверки
        // new word NewWord()
        // finish Finish()
        // bye Bye()
        // change user ChangeUser()
        return
    }

    public static String Start(){//заводим новый объект Dialog, загаданное слово, возвращает сообщение с загаданным словом
        //Dialog ()
    }
    public static String Letter(){// проверяем, есть ли буква в загаданном слове, возвращает сообщение с ответом да/нет,
        // если нет то + кол-во права на ошибку
        // ChekLetter()
    }
    public static String NewWord(){
        //NewHiddenWord(){}
    } // меняем поля в загаданном слове
    public static String Finish(){
        // FinishDiolog
    } // flag = false
    public static String Bye(){} // возвращает сообщение с прощанием
    public static String Flag(){}



}

 */